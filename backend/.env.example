# ===== 应用 =====
APP_NAME="AI 模拟面试官与职业规划系统"
# 默认 false（生产模式）：本地开发必须显式设为 true，否则默认密钥会被拒绝启动
DEBUG=true
# 日志级别：DEBUG / INFO / WARNING / ERROR（docker-compose 已透传）
LOG_LEVEL=INFO

# ===== 数据库 =====
# MVP 使用 SQLite；生产可切换 PostgreSQL，如:
# postgresql+psycopg://user:pass@localhost:5432/ai_interview
DATABASE_URL=sqlite:///./app.db

# ===== 向量库 =====
VECTOR_DB_PATH=./data/chroma
EMBEDDING_MODEL=BAAI/bge-m3

# ===== 安全 =====
# 生产环境（DEBUG=false）下：以下默认值/弱密钥将直接导致启动失败（强校验）。
# 本地开发（DEBUG=true）可使用默认值，但会收到告警提示。
# JWT_SECRET 需 ≥32 字符且非默认值；AES_KEY 需 32 字节且非默认值
JWT_SECRET=please-change-this-secret
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=10080
AES_KEY=please-change-this-aes-key-32bytes!

# ===== CORS =====
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# ===== 默认模型（用户未配置时可选）=====
DEFAULT_LLM_MODEL=gpt-4o-mini
DEFAULT_LLM_BASE_URL=https://api.openai.com/v1
